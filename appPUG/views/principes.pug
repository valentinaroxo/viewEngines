extends layout

block content
  h1 Príncipes Disney

  if preferencia && preferencia.length > 0
    ul
      each item in preferencia
        li
          strong= item.nome
          span=  " - " + item.filme
  else
    p Nenhum príncipe encontrado.